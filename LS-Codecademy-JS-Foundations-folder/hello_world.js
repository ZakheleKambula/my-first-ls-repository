console.log('Below are concept definitions');
let template_literal = 'advanced concatenation';



console.log(`Template literal is ${template_literal} (connecting strings) indicated by strings wrapped in backticks with embedded expressions or variables inside the string.`);